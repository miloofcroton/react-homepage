import React from 'react';
import styled from 'styled-components';

import Profile from './profile';

const Work = () => {
  return (
    <section>
      <Profile/>
    </section>
  );
};

export default Work;
